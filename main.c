#include <stdio.h>

int main () {
    int money, day;
    printf ("Specify the Deposit amount (RUR): ");
    scanf ("%d", money);
    printf ("Specify the Deposit term (days): ");
    scanf ("%d", day);
    return 0;
}
